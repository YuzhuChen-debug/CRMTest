<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="www.bjpowernode.crm.workbench.dao.ActivityDao">
  <select id="getActivityList" resultType="Activity">
    select
      a.id,
      a.name,
     u.name as owner,
     a.startDate,
     a.endDate
    from tbl_activity a
    join tbl_user u
    on a.owner=u.id
    <where>
      <if test="name!=''and name!=null">
        a.name like '%' #{name} '%'
      </if>

      <if test="owner!=''and owner!=null">
        and  u.name like '%' #{owner} '%'
      </if>
      <if test="startDate!=''and startDate!=null">
        and a.startDate &gt;  #{startDate}
      </if>
      <if test="endDate!=''and endDate!=null">
        and a.startDate &lt;  #{startDate}
      </if>
    </where>
    order by a.createTime desc
    limit #{skipCount},#{pageSize}

  </select>

  <select id="getCount" resultType="int">
    select count(*) from tbl_activity
    <where>
      <if test="name!=''and name!=null">
         a.name like '%' #{name} '%'
      </if>

      <if test="owner!=''and owner!=null">
        and  u.name like '%' #{owner} '%'
      </if>
      <if test="startDate!=''and startDate!=null">
        and a.startDate &gt;  #{startDate}
      </if>
      <if test="endDate!=''and endDate!=null">
        and a.startDate &lt;  #{startDate}
      </if>
    </where>
     order by a.createTime desc
    limit #{skipCount},#{pageSize}
  </select>

  <insert id="save">
      insert into tbl_activity(
        id,
        owner,
        name,
        startDate,
        endDate,
        cost,
        description,
        createTime,
        createBy
      )
      values(
        #{id},
        #{owner},
        #{name},
        #{startDate},
        #{endDate},
        #{cost},
        #{description},
        #{createTime},
        #{createBy}
      )
    </insert>
</mapper>